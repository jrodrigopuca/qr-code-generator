/**
 * @deprecated Use `@qr-plus/core` instead. This package is a backward-compatibility wrapper.
 *
 * ```diff
 * - import { generateQR } from "qr-pure";
 * + import { generateQR } from "@qr-plus/core";
 * ```
 */
export * from "@qr-plus/core";
